# Copy this to .env for local development (optional — defaults work out of the box)
# For production (Render/Railway/Fly), set these as environment variables in the dashboard.

PORT=3000
SESSION_SECRET=change-this-to-a-random-string-in-production
NODE_ENV=development

# On cloud platforms with a persistent volume mounted at /data:
# DATA_DIR=/data
